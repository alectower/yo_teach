<%= form_for to_do, remote: true do |f| %>

  <%= f.text_area :body, rows: 5, style: "width: 90%" %>
  <br/>

  <div class="actions">
    <%= f.submit 'Add To-do', class: 'btn' %>
  </div>

<% end %>
