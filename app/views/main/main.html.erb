<div class='main container-fluid'>
  <div id='messages'>
    <ul></ul>
  </div>
  <div class='row-fluid'>
    <%= render 'layouts/courses', locals: { courses: @services.fetch(:courses) } %>
    <%= render 'layouts/calendar', locals: { calendar: @services.fetch(:calendar) } %>
    <%= render 'layouts/notes', locals: { notes: @services.fetch(:notes) } %>
  </div>

  <div id="dialog" class="modal hide fade" tabindex="-1" role="dialog" aria-labelledby="dialogModalHeader" aria-hidden="true">
  <div class="modal-header">
    <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
    <h3 id="dialogModalHeader"></h3>
  </div>
  <div id='dialog-body' class="modal-body">
  </div>
  <div class="modal-footer">
    <button class="btn" data-dismiss="modal" aria-hidden="true">Cancel</button>
    <button id='submit' class="btn btn-primary">Create</button>
  </div>

</div>
